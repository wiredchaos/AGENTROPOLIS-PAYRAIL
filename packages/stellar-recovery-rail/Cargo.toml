[package]
name = "agentropolis-stellar-recovery-rail"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Fail-closed fiscal authorization policy for the Agentropolis Stellar Recovery Rail"

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"
